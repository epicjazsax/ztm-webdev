import { queryAllByText, render, screen } from '@testing-library/react';
import Card from './Card';

// describe('empty card', () => {
// 	const emptyCard = Card();

// 	it('contains a Name element', () => {
// 		expect(emptyCard).toHaveClass('cardName')
// 	})
// })

// describe('Nea Karlsson card', () => {
// 	const nea = {
// 		id: 1,
// 		name: 'Nea Karlsson',
// 		username: 'Mashtyx',
// 		email: 'urbanevader@dbd.bhvr',
// 	};
// 	const neaCard = Card(nea);
// 	const cardName = neaCard.getByText(nea.name)

// 	it('contains a Name element', () => {
// 		expect(cardName).toBe(nea.name)
// 	})

	// it('displays Neas name', () => {
	// 	expect(neaCard).toHaveTextContent(nea.name)
	// })
})